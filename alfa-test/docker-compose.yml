version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: alfa_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: alfa_db
      MYSQL_USER: alfa_user
      MYSQL_PASSWORD: alfa_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    command: --default-authentication-plugin=mysql_native_password
    networks:
      - alfa_network

volumes:
  mysql_data:

networks:
  alfa_network:
    driver: bridge 

    # instancia en 18.223.136.33